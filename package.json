{
  "name": "qiita-confetti",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build && yarn zip",
    "zip": "zip -r extension extension"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.2.9"
  }
}
