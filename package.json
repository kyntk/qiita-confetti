{
  "name": "qiita-confetti",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "build": "turbo run build",
    "zip": "zip -r extension extension"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.2.9"
  }
}
