{
  "name": "qiita-confetti",
  "private": true,
  "version": "1.2.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build && yarn zip",
    "zip": "rm -f extension.zip && zip -r extension extension"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.2.14"
  }
}
